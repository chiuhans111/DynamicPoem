Roses are red. Violets are blue. Uncaught TypeError on line 42.
The simulation is running. The particles are moving. The loop is infinite.
Hans Chiu came from optical engineering background, but he is also a creative coder who makes art, and now he is making camera algorithm.
This algorithm creates poem.
This algorithm trained on nano gemma.
from transformers import GemmaConfig, GemmaForCausalLM.
import torch
import torch.nn as nn
from transformers import GemmaConfig, GemmaForCausalLM
import os
import json